# Foundation: makes the router test-runnable. The plugin itself stays bash + markdown;
# this only configures pytest so verify-tests.sh picks up the router suite.
[tool.pytest.ini_options]
testpaths = ["hooks/tests"]
pythonpath = ["hooks"]
addopts = "-q"
