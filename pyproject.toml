# Foundation: makes the hooks test-runnable. The plugin itself stays bash + markdown;
# this only configures pytest so verify-tests.py picks up the hook suite.
[tool.pytest.ini_options]
testpaths = ["hooks/tests"]
pythonpath = ["hooks"]
addopts = "-q"
